import { ParticleTextEffect } from "./components/ui/particle-text-effect";

/**
 * Демо эффекта частиц. Сайт: добавьте к URL `?demo=particle` (см. `main.tsx`).
 * Либо импортируйте `ParticleTextEffect` в любой экран, например hero.
 */
export default function ParticleTextEffectDemo() {
  return <ParticleTextEffect />;
}
